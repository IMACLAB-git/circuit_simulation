name: Deploy to GitHub Pages

# Builds the static site and publishes it to Pages on every push to main.
# The Vite build uses relative asset paths, so the project sub-path that Pages
# serves from (https://<user>.github.io/<repo>/) needs no extra configuration.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a newer push cancel an in-flight deploy, but never interrupt one midway.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Reads the repository's Pages configuration. Pages itself is enabled
      # once in the repo settings (Settings > Pages > Source: GitHub Actions);
      # the workflow token is not allowed to create the site itself.
      - uses: actions/configure-pages@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # The solver checks run here too: a bad stamp should stop the deploy.
      - run: npm run check
      - run: npm run build:single
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
